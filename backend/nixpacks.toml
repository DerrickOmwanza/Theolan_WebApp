# Nixpacks configuration for Railway deployment
# https://docs.railway.app/docs/builders/nixpacks

[variables]
NODE_ENV = "production"

[build]
cmd = "npm install --only=production"

[start]
cmd = "npm start"
