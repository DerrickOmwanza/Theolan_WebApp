# Nixpacks configuration for Render deployment
# https://render.com/docs/buildpacks

[variables]
NODE_ENV = "production"

[build]
cmd = "cd backend && npm install --only=production"

[start]
cmd = "cd backend && npm start"
