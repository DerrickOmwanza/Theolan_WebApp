// Gallery Seeder - 20 images categorized based on analysis
// Admin will update: project_name, location, description

/**
 * @param {import('knex')} knex
 */
export async function seed(knex) {
  // Skip seeding - admin will upload images via admin panel
  console.log('Gallery image seeding skipped - admin will manage via panel');
}
